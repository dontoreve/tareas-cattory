"use client";

// Placeholder — will be migrated from login.html
export default function LoginPage() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-background-light">
      <p className="text-slate-500">Login — pendiente de migrar</p>
    </div>
  );
}
